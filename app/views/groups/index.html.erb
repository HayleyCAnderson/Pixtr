<h2>Groups</h2>

<ul>

  <% @groups.each do |group| %>
    <li>
      <%= link_to group.name, group_path(group.id) %>
    </li>
  <% end %>

  <li>
    <a href="<%= new_group_path %>">
      Add a Group
    </a>
  </li>
  
</ul>